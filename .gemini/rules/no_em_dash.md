# No Em Dash Rule

DO NOT use em dashes (—) anywhere in any copy, text content, titles, headings, or descriptions across this application. 

Instead of an em dash (—), always use:
- A standard hyphen with spaces (` - `)
- A colon (`:`)
- Or clean punctuation/spacing

This preference is permanently enforced across all project content and future updates.
